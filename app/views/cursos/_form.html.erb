<%= form_for(@curso, remote: true) do |f| %>
  <% if @curso.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@curso.errors.count, "error") %> prohibited this curso from being saved:</h2>

      <ul>
      <% @curso.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="row formpls">
    <div class="col-md-8">
      <%= f.text_field :name, placeholder: "Nombre", class: "form-control"  %>
    </div>

  </div>
<div class="btn_form">
  <%= f.submit "Crear Curso", class: 'btn btn-primary' %>
</div>
<% end %>
