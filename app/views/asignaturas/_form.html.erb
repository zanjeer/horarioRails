<%= form_for(@asignatura, remote: true) do |f| %>
  <% if @asignatura.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@asignatura.errors.count, "error") %> prohibited this asignatura from being saved:</h2>

      <ul>
      <% @asignatura.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<div class="row formpls">
  <div class="col-md-8">
    <!-- <%= f.label :name %><br> -->
    <%= f.text_field :name, placeholder: "Nombre", class: "form-control" %>
  </div>
  <div class="col-md-4">
    <%= f.label :lectiva %>
    <%= f.check_box :lectiva %>
  </div>

</div>
<div class="btn_form">

  <%= f.submit "Crear Asignatura", class: "btn btn-primary" %>
</div>
<% end %>
